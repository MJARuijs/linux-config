hl.config({
	input = {
		numlock_by_default = true,
		-- sensitivity = os.getenv("MOUSE_SENSITIVITY"),
		kb_options = "caps:swapescape,compose:ralt",
		repeat_delay = 150,
		repeat_rate = 35,
		follow_mouse = 1,
		touchpad = {
			natural_scroll = true,
		},
	},
})
